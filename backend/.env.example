# 배포 시 포트
PORT=22

# 컨테이너 안에서는 host=database, 호스트에서 직접 실행 시 host=localhost
DATABASE_URL=postgres://stepi:stepi@localhost:5432/stepi

# 프론트엔드 ↔ 백엔드 인증 키 (X-API-Key 헤더)
API_KEY=jinoo0306

# CORS는 코드에서 모든 origin을 허용하도록 설정되어 있음 — 별도 설정 불필요.
# (Express에서 cors() 호출 시 옵션 없음 = Access-Control-Allow-Origin: *)
